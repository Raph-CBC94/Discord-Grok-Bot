{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/discord-bot run build",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}
