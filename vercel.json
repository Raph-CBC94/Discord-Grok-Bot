{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "PORT=4173 BASE_PATH=/ pnpm --filter @workspace/discord-bot-panel run build",
  "outputDirectory": "artifacts/discord-bot-panel/dist/public",
  "functions": {
    "api/index.ts": {
      "maxDuration": 60
    }
  }
}